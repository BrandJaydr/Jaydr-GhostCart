# Error Log & Post-Mortem Register

This file logs system errors, unexpected failure modes, and post-mortem notes across agent execution runs within the **AOP-CORE v1.0** framework.

---

## 📋 Log Register

| ID | Date | Stage | Component | Error Description | Root Cause | Resolution / Prevention |
|---|---|---|---|---|---|---|
| ERR-000 | 2026-08-06 | Stage 0 | Documentation | Missing baseline documentation suite | Initial project setup phase | ✅ RESOLVED — Scribe initialized README.md, .logs/ register, and journal |
| ERR-001 | 2026-08-06 | Stage 1 | Scaffold | No application structure existed | Pre-scaffold state | ✅ RESOLVED — Forge scaffolded 35 files: Next.js app, Docker Compose, adapters, migrations, tests |
| ERR-002 | 2026-08-06 | Stage 1 | API route / imports | **Build-breaking import error** — `src/app/api/products/route.ts` line 4 imported `mockProduct` from `@/lib/adapters/mock`: the module file is actually `mock.adapter.ts`, and no `mockProduct` export exists (only the `mockAdapter` instance) | Scaffold referenced a wrong module path and a non-existent named export (an `@agent:atlas` handoff item) | ✅ RESOLVED — Corrected to `import { mockProduct } from '@/lib/adapters/mock.adapter'` and added a `mockProduct` fixture (`CanonicalProduct`) export to `mock.adapter.ts`; verified via `tsc` + tests |
| ERR-003 | 2026-08-06 | Stage 1 | Docker / infra | **Missing `Dockerfile.dev`** — the `app` service in `docker-compose.yml` (line 48) builds from `./Dockerfile.dev`, which did not exist, so `docker-compose up` could not build the app container | Scaffold gap: a documented/required file was never created | ✅ RESOLVED — Created `Dockerfile.dev` (node:20-alpine, `npm ci`, `next dev -H 0.0.0.0`) |
| ERR-004 | 2026-08-06 | Stage 1 | Worker / async pipeline | **`product.import` worker was a stub** — `src/worker/index.ts` registered the BullMQ worker but its processor threw `"not yet implemented"`; `POST /api/products` returned a **fake** 202 without enqueuing a real job, so no import pipeline existed | Unimplemented `@agent:atlas` handoff (TODO scaffold — Stage 1 stub policy) | ✅ RESOLVED — Implemented the processor: resolve adapter via factory → `importProduct()` → persist `products`+`jobs`+`audit_events` → return `{ productId, persisted }`, with graceful degradation (try/catch) + `recordJobFailure` on the `failed` event; wired `POST /api/products` to enqueue a real BullMQ job (lazy-import, guarded by `process.env.REDIS_URL`) |
| ERR-005 | 2026-08-06 | Stage 1 | Lint / CI gate | **Lint gate failure** — 2 `@typescript-eslint/consistent-type-imports` errors in `src/app/api/products/route.ts` and `src/app/api/listings/route.ts`: `NextRequest` imported as a value but used only as a type | Pre-existing scaffold violations that contradicted the enforced `--max-warnings 0` rule (repo was not actually passing its own lint gate) | ✅ RESOLVED — Changed both to `import type { NextRequest } from 'next/server'`; `npm run lint` now exits 0 |
| ERR-006 | 2026-08-06 | Stage 9 | Documentation | **Stale wiki entries** — `TECHNICAL_WIKI.md` §7.2 and §11 #7 asserted the `e2e/` directory "does not exist" when `e2e/smoke.test.ts` existed; §11 #1/#3 and §12.7 listed already-fixed items as still broken | Documentation drifted from the working tree | ✅ RESOLVED — Corrected §7.2, §11 #1/#3/#7, §12.5, §12.6, §12.7, §12.11 (marked RESOLVED / updated readiness) |
| ERR-007 | 2026-08-06 | Stage 1 | Config / package.json | **`package.json` became invalid JSON** while wiring `db:migrate` — the edit left a trailing comma on the last `scripts` entry, so `npm run lint` / `npm test` failed with `EJSONPARSE` ("expected double-quoted property name") | Trailing comma after the final `scripts` property (edits must not add a comma to the last object member) | ✅ RESOLVED — Removed the trailing comma; `package.json` re-parses and `tsc` / `lint` / `test` all pass |
| ERR-008 | 2026-08-09 | Stage 2 | Dependencies / Security | **Critical dependency vulnerabilities detected** — `npm audit` found 19 vulnerabilities (3 critical, 10 high, 5 moderate, 1 low). Critical: Vitest RCE vulnerabilities (GHSA-9crc-q9x8-hgqq, GHSA-5xrq-8626-4rwp). High: PostCSS XSS/path traversal (GHSA-qx2v-qp2m-jg93, GHSA-6g55-p6wh-862q, GHSA-r28c-9q8g-f849), Vite path traversal (GHSA-4w7w-66w2-5vf9, GHSA-fx2h-pf6j-xcff), Playwright vulnerabilities. Moderate: UUID buffer bounds check (GHSA-w5hq-g745-h8pq). | Outdated dependencies with known security vulnerabilities. Fixes available: vitest@4.1.10 (major), next@14.2.35, bullmq@5.81.3, @playwright/test@1.62.1. | ⚠️ PENDING — Requires dependency updates. Critical severity should be addressed immediately. |
| ERR-009 | 2026-08-09 | Stage 2 | Code Quality / TODOs | **Extensive TODO markers remain in codebase** — 168 TODO matches across 34 files in src/. Key areas: ProductCorrectionForm (6 TODOs), CorrectionField (6 TODOs), UI components (Button, Input, etc.), API routes (health, jobs, products), worker (concurrency tuning), and multiple page placeholders. | Scaffold TODOs not addressed during Stage 1-2 development. Many represent unimplemented features (form state, confidence visualization, revert functionality, design tokens). | ⚠️ PENDING — Technical debt accumulation. Should be triaged and addressed systematically. |
| ERR-010 | 2026-08-09 | Stage 2 | Logging / Observability | **Inconsistent logging patterns** — 50+ console.log/error/warn calls across src/. No structured logging framework. Worker uses console.warn for lifecycle events. API routes use console.error for error handling without correlation IDs or structured metadata. | No centralized logging solution. Makes debugging and monitoring difficult in production. Violates Production Blueprint §3.3 requirement for "structured logs, error tracking, health checks, and correlation IDs." | ⚠️ PENDING — Should implement structured logging (e.g., pino, winston) with correlation IDs. |
| ERR-011 | 2026-08-09 | Stage 2 | Security / Configuration | **Hardcoded credentials in .env.example** — .env.example contains placeholder values but no explicit warning about required secure values (NEXTAUTH_SECRET, DEV_SEED_PASSWORD). Database password uses weak default (ghostcart_dev). | Development configuration may accidentally be used in production. No secrets rotation process documented. | ⚠️ PENDING — Should add explicit warnings and require secure defaults. Implement secrets management per Production Blueprint §6.3. |
| ERR-012 | 2026-08-09 | Stage 2 | Database / Migrations | **Migration 0001 TODOs not completed** — 0001_init.sql contains TODO comments for RLS policies (line 7), billing_plan/settings (line 21), UNIQUE constraints (line 32), config encryption (line 44), identifiers/additional_image_urls/confidence (line 60). Some addressed in later migrations but not tracked. | TODO markers in production migrations create uncertainty about schema completeness. Risk of missing security constraints. | ⚠️ PENDING — Audit all migration TODOs and either complete or remove with justification. |
| ERR-013 | 2026-08-09 | Stage 2 | Test Coverage | **Limited test coverage** — Only 11 test files for 49 TypeScript files and 18 TSX files. No integration tests for worker processes. No E2E tests beyond smoke.test.ts. Many API routes lack test coverage. | Test gate may not catch regressions. Production Blueprint §Stage 1 Test Gate requires comprehensive coverage. | ⚠️ PENDING — Expand test coverage to critical paths: worker processors, API routes, RLS policies. |
| ERR-014 | 2026-08-09 | Stage 2 | Performance / Database | **Double query pattern in usage-limits.ts** — `checkUsageLimit` function made 2 separate DB queries: one to call `usage_limits.check_limit()` and another to fetch current usage values. This created unnecessary network latency and connection pool pressure. | Same pattern as previously fixed in products route. Repeated anti-pattern of separate data/count queries. | ✅ RESOLVED — Combined both queries into single CTE to reduce DB round trips from 2 to 1. ~50% reduction in database calls for usage limit checks. No new test failures introduced. |
| ERR-015 | 2026-08-12 | Stage 2 | Design System / Architecture | **Unauthorized UI library replacement** — Agent replaced HeroUI components with Radix UI primitives + Tailwind CSS without following project plan. This violated documented strategy in FRONTEND_DEVELOPMENT_PLAN.md, UI_LIBRARY_MAPPING_TABLE.md, and IMPLEMENTATION_DEV_TABLE.md which specify HeroUI as the primary component library. The replacement was made due to HeroUI v3 requiring React 19+, but the project uses React 18.3.1. | Agent failed to follow established project documentation and made unilateral architectural decision. React 19 compatibility issue with HeroUI v3 should have been resolved through proper dependency resolution (HeroUI v2 supports React 18+) rather than replacing the entire component library. The agent also ignored user's explicit preference for enterprise-grade libraries (HeroUI, MagicUI, Lightswind UI) and PRISM hierarchy which places React Spectrum (#1) and React Aria (#2) above shadcn/ui (#3). | ✅ RESOLVED — Reverted Radix UI changes. Documented HeroUI v3 compatibility issue and HeroUI v2 solution. Updated all documentation to reflect HeroUI v2 as primary library with React 18 compatibility. Established clear decision process for future library changes. |
| ERR-016 | 2026-08-12 | Stage 2 | API authorization / tenancy | **API routes still use a fixed development tenant and lack route-level session/RBAC checks.** `rg` found `DEV_TENANT_ID` in product, listing, job, alert, repricing, AI, eBay, feedback, dashboard, and admin handlers; only the dashboard layout performs a session redirect. `/api/admin/beta-users` additionally accepts caller-supplied `tenantId` and `userId`. | Authentication was implemented as a page/layout guard, while API handlers retained Stage 1 development placeholders. Direct `db.query` calls also bypass the documented `withTenant()` transaction/RLS context. | ⚠️ PENDING — Block production/Stage 5 expansion on a shared server-side auth + role guard, session-derived tenant IDs, and cross-tenant integration tests. Do not expose detailed exploit instructions in public logs. |
| ERR-017 | 2026-08-12 | Stage 2 | Observability / webhooks | **Recurring uncorrelated and potentially over-broad error logging remains.** API/worker/library code continues to use raw `console.*` calls without request/job correlation fields; `src/app/api/ebay/webhook/route.ts` logs the full order payload. | No centralized structured logger or redaction policy is enforced at the logging boundary. | ⚠️ PENDING — Introduce a small shared structured event helper with correlation IDs and redact payloads; add tests that assert sensitive webhook fields are not emitted. |
| ERR-018 | 2026-08-12 | Stage 2 | Verification / test harness | **Vitest cannot start in the current sandbox.** `npm test -- --reporter=basic` fails while loading `vitest.config.ts` with `Cannot read directory "../../..": Access is denied`, so the suite did not execute. | The test runner/esbuild config resolution is blocked by the environment boundary before test collection. | ⚠️ PENDING — Re-run in a normal workspace/CI runner; treat a green result as unverified until the config loads and test counts are recorded. |
| ERR-019 | 2026-08-12 | Stage 2 | UI Components / AppShell | **HeroUI Layout API mismatch in AppShell.tsx** — `src/components/layout/AppShell.tsx` uses `Layout.Header`, `Layout.Sider`, `Layout.Content` subcomponents that don't exist in HeroUI v2.8.10. This causes runtime error "Cannot read properties of undefined (reading 'Header')". | Code assumed Ant Design-style Layout subcomponents that HeroUI doesn't provide. UI_LIBRARY_MAPPING_TABLE.md correctly specifies custom Tailwind layout, but AppShell implementation doesn't match this guidance. | ✅ RESOLVED — Replaced HeroUI Layout with custom Tailwind layout (CSS Grid/Flex) as specified in UI_LIBRARY_MAPPING_TABLE.md. Updated AppShell, Sidebar, TopNav components with proper semantic HTML and Lucide React icons. Toast provider fixed with custom accessible implementation. |
| ERR-020 | 2026-08-12 | Stage 4 / deployment | **Deployment posture is development-only and documentation was at risk of conflating it with production readiness.** The repository has `docker-compose.yml` plus `Dockerfile.dev` for a local Next.js development server, PostgreSQL 16, and Redis 7. It has no WordPress service/plugin integration, no production Dockerfile, no worker service in Compose, and no configured remote host or deployment workflow. Docker status could not be queried from this sandbox because access to the Docker socket is denied. | The original WordPress concept remains in historical documents, while the current modular-monolith code evolved toward Docker Compose. Production topology was deferred without a single explicit deployment decision record. | ⚠️ PENDING — Treat Docker Compose as the current local-development method only. Before a pilot deployment, create a production image and production Compose/host configuration with separate web and worker processes, production health checks, secure secrets, backups/restore, and rollback instructions. |
| ERR-021 | 2026-08-17 | Stage 2 | UI Components / Tailwind Config | **HeroUI components render completely unstyled & text overlaps** — Form fields (e.g., Product URL input) display labels colliding directly over placeholder text with no input box border or background, and buttons collapse to plain unstyled text. | `tailwind.config.ts` was missing the `@heroui/react` `heroui()` plugin and `./node_modules/@heroui/theme/dist/**/*.{js,ts,jsx,tsx}` in its `content` path, causing Tailwind to purge all HeroUI component styles. | ✅ RESOLVED — Configured `heroui()` plugin and included `@heroui/theme` content path in `tailwind.config.ts`. Verified input boxes and buttons render with proper borders, padding, and layout bounds. |

| ERR-022 | 2026-08-17 | Stage 2 | UI / API contract | **ImportForm <-> POST /api/products integration mismatch (hidden).** ImportForm.tsx posts { sourceUrl, adapter }, but ProductImportSchema expects { url, supplierId, idempotencyKey }, so the URL submit is undefined -> 400 and the import form always fails. On success the form reads response.data.id, but the API returns data.data.jobId (status: queued), yielding a broken /products/undefined/review link. | Client was written against the pre-BullMQ contract and not updated when POST was switched to enqueuing jobs. | ✅ RESOLVED 2026-08-19 (Fix 1) — ImportForm now posts { url, supplierId, idempotencyKey } via a supplier selector, reads data.jobId, and a regression test asserts ProductImportSchema compatibility. |
| ERR-023 | 2026-08-17 | Stage 2 | Frontend / missing routes | 7 nav destinations have no page.tsx and 404: Sidebar /jobs, /repricing, /settings/general, /settings/marketplaces, /settings/suppliers; TopNav user menu /profile and /sign-out. Backend APIs for these exist (jobs, repricing, auth/keys). CommandPalette also pushes to the missing routes. | Frontend scaffold never built pages for already-implemented backends (API/UI imbalance). | ✅ RESOLVED 2026-08-19 (Fix 2) — placeholder page.tsx added for /jobs, /repricing, /settings/{general,marketplaces,suppliers}, /profile; /sign-out retired (logout now ends the session). |
| ERR-024 | 2026-08-17 | Stage 2 | Auth / session | Logout is non-functional - no signOut() (next-auth/react) call exists anywhere; TopNav Log out navigates to /sign-out (missing) instead of ending the session. Stale sessions cannot be terminated from the UI. | Scaffold leftover: logout routed to a page that was never created. | ✅ RESOLVED 2026-08-19 (Fix 2) — TopNav logout now calls signOut({ callbackUrl: '/sign-in' }); dead /sign-out nav removed. |
| ERR-025 | 2026-08-19 | Stage 2 | Lint / CI gate | `npm run lint` (eslint . --max-warnings 0) does not exit 0: PRE-EXISTING errors in `Prism Working/Template and Samples/*` (react/no-unescaped-entities, no-img-element) and ~40 `src/app/api/*` routes (no-unused-vars, no-explicit-any, consistent-type-imports, no-console). These predate the Fix 1/Fix 2 work — `tsc --noEmit` exits 0 and none of the Fix 2 changed/new files are flagged. | Legacy imported sample template plus scaffold API routes that predate the enforced `--max-warnings 0` gate. | ⚠️ PENDING — separate hygiene track; subsumed by SEC-007 (structured logging / type-safety remediation). Fix 2 verified its 7 changed/new files are lint-clean. |
| ERR-026 | 2026-08-21 | Stage 2 | Build / Component | **Inline Zod import causes build errors** — Using `import { z } from 'zod'` inside a React component function body caused compilation/build errors. | Zod imports must be top-level in the module. Inline imports violate ES module rules and React component isolation. | ✅ RESOLVED — Moved `import { z } from 'zod'` to the top of `src/app/(dashboard)/products/editor/page.tsx`. |
| ERR-028 | 2026-08-21 | Stage 2 | Build / Blank screen | **Blank screen on every page with `npm run dev`.** Server returned full HTML (200) but the browser showed nothing. Dev log revealed `Module not found: Can't resolve '../env.js'` from `src/lib/db/index.ts` (and later `'./logger.js'` from `src/lib/env.ts`). TypeScript's `.js`→`.ts` resolution masks these at `tsc` time, but webpack/Next refuses to resolve `.js` specifiers when only the `.ts` file exists, so the module graph fails to compile at request time → 500 → blank render. | Stale `.js`-suffixed relative imports pointing to `.ts`-only modules — introduced after a rename/refactor (`db`/`logger`/`env` moved to `.ts` without updating specifiers). | ✅ RESOLVED 2026-08-21 — Changed `src/lib/db/index.ts:3` `'../env.js'` → `'../env'` and `src/lib/env.ts:1` `'./logger.js'` → `'./logger'`. Verified: dev compiles (2411 modules), `GET /sign-in` → 200 with full HTML (`HAS_TEXT=True`), `npx tsc --noEmit` exit 0, eslint clean on changed files. Also fixed `SignInForm` `onChange`→`onValueChange` (login state now updates) — same root class: prop/specifier drift. |
| ERR-027 | 2026-08-21 | Stage 2 / Deploy | Auth / Build | **`headers was called outside a request scope` during production build.** `(dashboard)/layout.tsx` calls `getServerSession()` (reads cookies via `headers()`); Next 14 static pre-render of the layout at `npm run build` runs before any request exists, so `headers()` throws. `force-dynamic` was attempted but is an unreliable workaround in 14.2.x and is no longer present in the tree. Surfaces on Railway/Render/AWS (build then start) — dashboard errors at runtime in production. | `getServerSession` in a layout requires a live request scope, absent during build-time static generation. | ⚠️ PENDING (logged pre-deployment) — recommended fix: add `src/middleware.ts` using next-auth `withAuth` to enforce auth at the request boundary, and simplify `(dashboard)/layout.tsx` to render `AppShell`/`ToastProvider` only (drop `getServerSession` + `redirect`). Middleware runs in a real request context identically in dev/build/prod. No code change yet |
| ERR-028 | 2026-08-21 | Stage 2 | Syntax / Build | **Unclosed `useEffect` block in `src/app/(dashboard)/products/editor/page.tsx`** — missing closing brackets and dependency array, causing `TS1005: ')' expected` at the end of the file. | A code edit in the CSV Editor page left the `useEffect` hook block unclosed before `validateRow` function declaration. | ✅ RESOLVED — Closed the `useEffect` hook block properly with dependency array `[activeCell]`. Verified compilation passes successfully. |